bin/
*.exe
*.test
*.out
coverage.*
.env
.DS_Store
.coverdata/

# Everything in docs/ — the specs and the cross-session working state — stays
# local, along with the harness config. The spec-drift test skips when the
# spec is absent, so a clean clone still passes the full gate.
docs/
.claude/
CLAUDE.md
